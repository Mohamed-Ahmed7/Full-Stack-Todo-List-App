import TodoList from "../components/TodoList";

const HomePage = () => {
  return (
    <section className="max-w-2xl mx-auto border-2 border-red">
      <TodoList />
    </section>
  );
};

export default HomePage;
